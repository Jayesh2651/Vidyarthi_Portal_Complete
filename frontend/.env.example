# Leave blank to use the Vercel same-origin `/api` proxy.
VITE_API_BASE_URL=

# Keep `/` for Vercel deployments.
# Only switch this to `/static/` when you intentionally build the SPA for Django to serve.
VITE_BASE_PATH=/
